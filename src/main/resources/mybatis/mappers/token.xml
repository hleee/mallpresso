<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.token">
	<resultMap id="tokenResult" type="tokenVO">
		<result property="logInToken" column="token" />
		<result property="email" column="email" />
		<result property="createdAt" column="created_at" />
	</resultMap>

	<insert id="insertOneLogInToken" parameterType="tokenVO">
    <![CDATA[
      INSERT INTO token(token, email)
      VALUES(#{logInToken}, #{email})
    ]]>
	</insert>

	<select id="selectOneRowByLogInToken" resultMap="tokenResult"
		parameterType="tokenVO">
    <![CDATA[
      SELECT * FROM token
      WHERE token = #{logInToken}
    ]]>
	</select>

</mapper>
	